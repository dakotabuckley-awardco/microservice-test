microservice:
  name: bouncer-1

dev:
  enabled: "true"
  values:
    resources:
      limits:
        cpu: "1200m"
        memory: "260Mi"
  targetRegions:
    - region: westus2
      values:
        resources:
          limits:
            cpu: "700m"
            memory: "256Mi"
    - region: eastus2
  environment: dev
  project: default